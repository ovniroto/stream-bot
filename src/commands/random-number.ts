// deno-lint-ignore-file

import { ICommandPlatform, IDatabaseCommand } from '../interfaces/commands.ts'
import { IChat, IClient } from '../interfaces/twitch.ts'

const randomNumberCommand = async (command: IDatabaseCommand, chat: IChat, client: IClient, platform: ICommandPlatform) => {

}

export default randomNumberCommand