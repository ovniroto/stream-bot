// deno-lint-ignore-file

import { ICommandChat, ICommandClient, ICommandPlatform, ICommandDatabase } from '@/interfaces/commands.ts'

const chatGPTModCommand = async (command: ICommandDatabase, chat: ICommandChat, client: ICommandClient, platform: ICommandPlatform) => {

}

export default chatGPTModCommand