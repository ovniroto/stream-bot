// deno-lint-ignore-file

import { ICommandPlatform, ICommandDatabase } from '../interfaces/commands.ts'
import { IChat, IClient } from '../interfaces/twitch.ts'
import {  } from '../interfaces/spotify.ts'

const spotifyCommand = async (command: ICommandDatabase, chat: IChat, client: IClient, platform: ICommandPlatform) => {



}

export default spotifyCommand